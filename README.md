# Spectra API
## Introduction
A high-performance, multi-tenant API gateway for managing microservices.
## Getting Started
1. Run `go mod init spectra-api`
2. Run `go get` to install dependencies
3. Run `go build` to compile the code
4. Run `go run .` to start the server